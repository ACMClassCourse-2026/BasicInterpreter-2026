# Lexer

## 概述

`Lexer` 负责将一行源码拆分为 `TokenStream`。关键字只识别大写形式，普通标识符保留原始大小写；REM 后的内容整体记为一个 `RemText`。非法字符、非法数值形式和未闭合字符串会产生语法错误。

`Lexer` 不保存状态，也没有数据成员。

## 公有成员函数

| 名称 | 作用 |
| :-: | :-: |
| `tokenize` | 扫描给定源码，生成带原始文本和列号的 token 序列，并在末尾添加 `EndOfInput` |
